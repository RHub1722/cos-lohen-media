# Рабочий каталог бенчмарка: сгенерированные исходники и срезы.
# Полностью воспроизводится командой `python benchmark/bench.py`.
.work/

# Результаты в results/ версионируются намеренно — их сравнивают между машинами.
