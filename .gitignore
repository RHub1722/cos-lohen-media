# Результаты сборки лежат в output/ и не версионируются — см. output/.gitignore.
# Они полностью воспроизводятся командой `python src/build.py`.

# Python
__pycache__/
*.py[cod]
*.pyo

# Виртуальные окружения (проекту не нужны, но на случай локальных экспериментов)
.venv/
venv/
env/

# Звук официального трейлера: исходник для аукционной половины сценария v3.
# В репозиторий не попадает — это чужой материал, и 50 МБ WAV в истории не нужны.
# Воспроизводится командой из docs/status/2026-07-30-v3a.md.
assets/trailer/

# Служебное
.DS_Store
Thumbs.db
desktop.ini
