# Задача на 2026-07-31 — сгенерировать голоса и вложить их в дорожку

| | |
|---|---|
| Отправная точка | [docs/status/2026-07-30-v2c.md](../status/2026-07-30-v2c.md) |
| ТЗ по репликам | `scenario/CLAUDE_HANDOFF_LOHEN_AUDIO_PROJECT.md`, §4 и §5 |
| Куда класть | `assets/voices/` |
| Итог | пересобранный `master_v2.wav` с семью репликами |

Сборка уже умеет всё, что нужно: длительность реплики берётся из файла,
приглушение фона под голосом включается само, панорама покупателя и центрирование
Лоэна прописаны. Ничего в коде менять не придётся — только положить файлы и
пересобрать.

---

## 1. Что генерировать

Имена файлов должны совпадать **точно** — сборка ищет их по имени.

Колонка «слот» — сколько секунд есть до следующего события. Если реплика окажется
длиннее, она наедет на следующую; валидатор об этом предупредит, но сам ничего не
подвинет.

### 1.1. `auctioneer_intro.wav`

```text
Ladies and gentlemen... our final lot of the evening.
A weapon without equal.
```

Допустима сокращённая версия: только первая строка.

| | |
|---|---|
| Старт в сценарии | **00:00.000** — но см. раздел 4, это открытый вопрос |
| Слот | 20.0 с от нуля, либо 12.0 с если сдвинуть старт на 00:08.000 |
| Подача | объёмно и официально, лёгкая реверберация большого зала |

По §4 сцена 00:00–00:08 немая: зрителю дают рассмотреть костюм, «никаких громких
эффектов». Реплика должна начинаться в 00:08.000.

### 1.2. `buyer_suspicious.wav`

```text
He looks far too calm.
```

| | |
|---|---|
| Старт | 00:20.000 |
| Слот | 9.0 с |
| Подача | голос из толпы, панорама уже стоит на −0.2 (слегка слева) |

### 1.3. `lohen_notice.wav`

```text
I was beginning to think... no one would notice.
```

| | |
|---|---|
| Старт | 00:29.000 |
| Слот | 10.3 с (до щелчка оков в 00:39.300) |
| Подача | slowly, quietly, with restrained amusement, a sly undertone and a faint predatory smile |

Русская смысловая версия для сверки: «Я уже начал думать... что никто не заметит.»

Пауза после «think» держится — на ней по §4 Лоэн опускает взгляд. Никакого
рычания и карикатурного злодейства.

### 1.4. `auctioneer_sold.wav`

```text
Sold.
```

| | |
|---|---|
| Старт | 00:40.000 |
| Слот | 8.0 с, но реально нужно уложиться в 0.5 с — в 00:40.500 бьёт молоток |
| Подача | резко, объявление |

### 1.5. `lohen_reveal.wav`

```text
You misunderstand.

I was never the merchandise.
```

Альтернатива из §4: «You misunderstand. Today, I am not the one being sold.»
Основным считается первый вариант.

| | |
|---|---|
| Старт | 00:48.000 |
| Слот | 8.8 с (до вздоха толпы в 00:56.800) |
| Подача | спокойно; между фразами короткая пауза — на ней Лоэн освобождает руки |

### 1.6. `lohen_final_question.wav`

```text
Was that all?
```

| | |
|---|---|
| Старт | 01:20.000 |
| Слот | 9.0 с |
| Подача | спокойно, без крика, с лёгким разочарованием |

### 1.7. `lohen_final_line.wav`

```text
How disappointing.
```

| | |
|---|---|
| Старт | 01:29.000 |
| Слот | 8.5 с (до ледяного удара в 01:37.500) |
| Подача | холодное разочарование |

Под этой репликой лёд приглушается на 5 dB (группа `ice`) — специально, чтобы
голос не перекрывался.

### Характер Лоэна для всех его реплик (§2)

Внешне спокойный и вежливый, внутренне хищный и самоуверенный. Не испуган,
заранее контролирует ситуацию, получает удовольствие от риска и превосходства.

---

## 2. Требования к файлам

```text
WAV, 48 kHz, 16-bit или 24-bit
mono или stereo — сборка примет и то и то
```

- MP3 как источник не использовать (§8).
- Не нормализовать каждую реплику до максимума, сохранить динамику.
- Тишину по краям обрезать не нужно — сборка это сделает сама, если поставить
  событию `trim_silence`.
- Другую частоту или моно сборка примет, приведёт сама и напишет предупреждение.

---

## 3. Главная ловушка: голос в 0 dBFS уронит громкость всей дорожки

Генераторы обычно выдают речь с пиком около 0 dBFS. Если положить такой файл как
есть, при `gain_db: 0` и без `normalize_source` произойдёт вот что:

| | пик микса | линейный прирост | итог |
|---|---|---|---|
| Сейчас, без голосов | −10.8 dBFS | **+8.5 dB** | −18.4 LUFS |
| Голос в 0 dBFS | 0.0 dBFS | **−2.3 dB** | около −29 LUFS |
| Голос с `normalize_source` | −9.6 dBFS | **+7.3 dB** | примерно как сейчас |

Причина: включён `output.preserve_dynamics`, и максимальный линейный прирост
равен `потолок True Peak − пик микса − 0.5`. Голос на полной шкале съедает весь
запас, и вместо усиления дорожка получает ослабление на 2.3 dB.

**Поэтому первым делом каждому голосовому событию нужно поставить:**

```json
"normalize_source": true
```

Тогда реплика встанет на ту же абсолютную шкалу, что и все SFX: при `gain_db: 0`
это пик −9.6 dBFS. Для сравнения, ледяной удар стоит на −13.6 dBFS, то есть
голоса окажутся на 4 dB выше самого громкого акцента — ровно как требует
иерархия §10, где голоса имеют приоритет 1.

---

## 4. Открытый вопрос до начала работы

`auctioneer_intro` стоит в **00:00.000**, а по §4 сцена 00:00–00:08 должна быть
немой и реплика принадлежит окну 00:08.000–00:20.000.

Решить до генерации, потому что от этого зависит нужная длина файла: 20 секунд
или 12. Правится одним полем `start` в `scenario/timeline.json`.

---

## 5. Порядок работы

1. Сгенерировать семь файлов, положить в `assets/voices/`.

2. Проверить, что нашлись и какой они длительности, ещё до рендера:

   ```bash
   python src/build.py --validate-only
   ```

   Ожидается: предупреждений про отсутствующие голоса больше нет. Если реплика
   не влезает в слот — будет предупреждение с конкретными таймкодами.

3. Поставить `"normalize_source": true` всем семи голосовым событиям (раздел 3).

4. Собрать:

   ```bash
   python src/build.py --mp3
   ```

   Суффикс `v2` подставится сам из `output.default_suffix`.

5. Пройти проверку из раздела 6.

---

## 6. Что проверить после сборки и какой результат ожидается

| # | Что | Ожидаемый результат |
|---|---|---|
| 1 | `master_v2.wav` создан | длительность ровно `103.000000`, 48 kHz, stereo, PCM 24-bit |
| 2 | Отсутствующие ассеты в отчёте | `missing_optional_assets` пуст (или только музыка) |
| 3 | Режим нормализации | `loudness.normalization_type` = `linear`. Если стало `dynamic` — вернуться к разделу 3 |
| 4 | Громкость | ближе к −16 LUFS, чем сейчас: микс стал плотнее, crest уменьшился. Если ушло к −29 — не выставлен `normalize_source` |
| 5 | True Peak | не выше −1.5 dBTP |
| 6 | Приглушение под репликами | в `render-report-v2.json` → `ducking.voice_windows` семь интервалов. Фон под каждым уходит на −6 dB, толпа на −8 dB |
| 7 | Разборчивость | каждое слово разбирается на фоне зала и толпы |
| 8 | `auctioneer_intro` | не наезжает на `buyer_suspicious` в 00:20.000 |
| 9 | `auctioneer_sold` и молоток | «Sold» успевает закончиться до удара в 00:40.500 либо удар ложится на конец слова осмысленно |
| 10 | `lohen_final_line` и лёд | реплика не перекрыта расползанием льда; под голосом лёд уходит на −5 dB |
| 11 | Немая сцена 00:00–00:08 | если старт реплики оставлен в 00:00 — тишины там больше нет, это осознанное решение по разделу 4 |

---

## 7. Что делать не нужно

- Не подгонять скорость речи под слоты автоматически (§5). Если реплика не
  влезает — либо перегенерировать короче, либо сдвинуть таймкоды в JSON.
- Не менять сюжет и тексты реплик без отдельного согласования.
- Не трогать боевые таймкоды: они остаются черновыми до записи видео
  хореографии (§14).
- Не выбирать музыку — по §18 это следующий этап, после хореографии.

---

## 8. После этой задачи

По §18 следующий шаг — записать видео черновой хореографии, зафиксировать
фактические таймкоды взмахов и попаданий и передвинуть боевые SFX под
исполнителя. Текущие тайминги боя подстроены не под человека, а под технический
черновик.

Остаются открытыми решения из раздела 3 точки
[2026-07-30-v2c](../status/2026-07-30-v2c.md): недобор громкости до −16 LUFS,
провал молотка и щелчка по громкости, разница фоновых залов в 34 dB, дыра в слое
толпы 01:03–01:12.
