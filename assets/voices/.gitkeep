# Папка обязана существовать в клоне: validator.py проверяет её наличие
# (REQUIRED_DIRS в src/validator.py), а git не хранит пустые каталоги.
# Файл можно удалить, как только в папке появится настоящее содержимое.
